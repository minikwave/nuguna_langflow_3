global:
  scrape_interval: 5s  # 데이터 수집 주기

scrape_configs:
  - job_name: "app_metrics"
    static_configs:
      - targets: ["app:8000"]  # FastAPI 메트릭 수집
  - job_name: "pushgateway"
    static_configs:
      - targets: ["pushgateway:9091"]  # Prometheus PushGateway
